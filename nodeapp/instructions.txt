# Create nodeapp folder
touch app.js # Create node.js application
npm i expres # Install Express for node.js application
# Create Dockerfile

FROM node:14
WORKDIR /app
COPY package.json ./
RUN npm install
COPY . .
EXPOSE 8080
CMD ["npm","start"]

# Create terraform folder
# Make main.tf 
# Make variables.tf 

Create read only IAM role read only access for Amazon EC2 container registry 
Make s3 bucket for terraform state
make terraform file and variables file
Make ssh key by using keygen in terminal

set up secrets in github

AWS_ACCESS_KEY	
AWS_ACCESS_KEY_ID
AWS_SSH_KEY_PRIVATE
AWS_SSH_KEY_PUBLIC
AWS_TF_STATE_BUCKET_NAME

Create yaml file


server
ECR-Read-Only


udAlSJM1tepF/6LDgOPbz/plpIkFz7yU/5xZKMbB

AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
  TF_STATE_BUCKET_NAME: ${{ secrets.AWS_TF_STATE_BUCKET_NAME }}
  PRIVATE_SSH_KEY: ${{ secrets.AWS_SSH_KEY_PRIVATE }}
  PUBLIC_SSH_KEY: ${{ secrets.AWS_SSH_KEY_PUBLIC }}
  AWS_REGION: us-east-1